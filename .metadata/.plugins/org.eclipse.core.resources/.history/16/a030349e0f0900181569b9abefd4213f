import java.io.FileReader;
import java.io.IOException;
import java.io.Reader;
import java.lang.reflect.Type;
import java.util.*;

import com.google.gson.*;
import com.google.gson.reflect.TypeToken;

public class FileHandler {
	ArrayList<Patient> patients = new ArrayList<>();
	// TODO add file selector to upload a json file.
	private static String fileLocation = ".\\assignment_1_example.json";

	private class Readings {
		@SuppressWarnings("unused")
		ArrayList<Reading> patient_readings;
	}

	public static void main(String[] args) {
		Gson gson = new GsonBuilder().serializeNulls().create();

		try (Reader fileReader = new FileReader(fileLocation)) {
			Readings readingList = gson.fromJson(fileReader, Readings.class);
			System.out.println(readingList.patient_readings);

		} catch (IOException e) {
			e.printStackTrace();
		}

	}

}
