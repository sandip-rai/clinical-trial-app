import java.io.FileReader;
import java.io.IOException;
import java.io.Reader;
import java.lang.reflect.Type;
import java.util.*;

import com.google.gson.*;
import com.google.gson.reflect.TypeToken;

public class FileHandler {
	ArrayList<Patient> patients = new ArrayList<>();
	// TODO add file selector to upload a json file.
	String fileLocation = ".\\assignment_1_example.json";

	private class Readings {
		@SuppressWarnings("unused")
		ArrayList<Reading> patient_readings;
	}

	public static void main(String[] args) {
		Gson gson = new GsonBuilder().serializeNulls().create();

		try (Reader reader = new FileReader(fileLocation)) {
			Type readingListType = new TypeToken<ArrayList<Reading>>() {
			}.getType();
			ArrayList<Reading> readingList = gson.fromJson(json, classOfT)

		} catch (IOException e) {
			e.printStackTrace();
		}

	}

}
